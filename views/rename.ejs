<!DOCTYPE html>
<html>
<head><title>Rename Item</title></head>
<body>
    <% if (locals.user) { %>
        <h1>Rename: <%= item.originalName %></h1>
        <form action="/rename/<%= item.id %>" method="POST">
            <label for="newName">New Name:</label>
            <input type="text" id="newName" name="newName" value="<%= item.originalName %>" required>
            <button type="submit">Save</button>
        </form>
        <br>
        <!-- Link back to the parent folder -->
        <a href="<%= item.parentId ? '/folder/' + item.parentId : '/' %>">Cancel</a>
    <% } else { %>
        <p>Please log in.</p>
        <a href="/">Home</a>
    <% } %>
</body>
</html>